// placeholder para resultados segregados vía rutas (p. ej. /search?q=...)
export function SearchResultsPage() {
  const root = document.createElement("div");
  root.textContent = "Search results - placeholder";
  return root;
}
